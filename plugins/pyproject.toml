[project]
name = "forzium-plugin"
version = "0.1.1"
description = "Forzium plugin providing scaffold command"
requires-python = ">=3.11"

[project.entry-points."forzium.plugins"]
scaffold = "forzium_plugin:register"