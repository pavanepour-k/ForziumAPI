[project]
name = "forzium-sample-plugin"
version = "0.1.4"
description = "Sample Forzium CLI plugin demonstrating entry point registration"
requires-python = ">=3.12"

[project.entry-points."forzium.plugins"]
hello = "forzium_sample_plugin:register"