[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "forzium-sample-plugin"
version = "0.1.4"
description = "Minimal sample plugin used for version consistency tests"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = []